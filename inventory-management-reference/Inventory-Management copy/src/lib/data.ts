import type { InventoryItem, PurchaseOrder, Notification, Dealer } from './types';
import { AlertTriangle, CheckCircle, Truck } from 'lucide-react';

// This file contains mock data and can be removed once Firebase is fully integrated.

export const inventoryItems: InventoryItem[] = [];
export const purchaseOrders: PurchaseOrder[] = [];
export const notifications: Notification[] = [];
export const dealers: Dealer[] = [];
